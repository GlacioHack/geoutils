name: geoutils
channels:
  - conda-forge
dependencies:
  - python=3
  - geopandas >= 0.10.0
  - matplotlib
  - pyproj
  - rasterio
  - scipy
  - pip
  - tqdm
  - pip:

    - "typing-extensions; python_version < '3.8'"
