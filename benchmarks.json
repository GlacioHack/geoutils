{
    "asv_suite.comparisons.DaskIdwNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskIdwNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskIdwNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskIdwNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskIdwScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskIdwScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskIdwScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskIdwScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskLinearScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskLinearScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskLinearScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskLinearScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskLinearScipyInterpolationPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskLinearScipyInterpolationPointCount.time_operation",
        "number": 1,
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskLinearScipyInterpolationPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskLinearScipyInterpolationPointCount.track_end_to_end_time_s",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMeanNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMeanNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMeanNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMeanNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMeanScipyFilterChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMeanScipyFilterChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMeanScipyFilterChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMeanScipyFilterChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMeanScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMeanScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMeanScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMeanScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskMomentsNumpySparseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskNearestNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskNearestNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskNearestNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskNearestNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskNearestRasterioReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskNearestRasterioReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskNearestRasterioReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskNearestRasterioReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskNearestScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskNearestScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskNearestScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskNearestScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskRasterioGeometryStitchPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskRasterioGeometryStitchPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskRasterioGeometryStitchPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioGeometryStitchPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskRasterioGeometryStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioGeometryStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskRasterioLabelStitchPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskRasterioLabelStitchPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskRasterioLabelUnionPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskRasterioLabelUnionPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskRasterioLabelUnionPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioLabelUnionPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskRasterioLabelUnionPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioLabelUnionPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskRasterioRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskRasterioRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskRasterioRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskSequentialSubsampleSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskSequentialSubsampleSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskSequentialSubsampleSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskSequentialSubsampleSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskSequentialSubsampleSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskSequentialSubsampleSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.DaskTopkSubsampleSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.DaskTopkSubsampleSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.DaskTopkSubsampleSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskTopkSubsampleSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.DaskTopkSubsampleSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.DaskTopkSubsampleSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerIdwNumbaGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerIdwNumbaGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerIdwNumbaGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerIdwNumbaGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerIdwNumbaGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerIdwNumbaGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerIdwScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerIdwScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerIdwScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerIdwScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerLinearScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerLinearScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerLinearScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerLinearScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerLinearScipyInterpolationPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerLinearScipyInterpolationPointCount.time_operation",
        "number": 1,
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerLinearScipyInterpolationPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerLinearScipyInterpolationPointCount.track_end_to_end_time_s",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerMeanNumbaGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerMeanNumbaGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerMeanNumbaGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerMeanNumbaGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerMeanNumbaGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerMeanNumbaGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerMeanScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerMeanScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerMeanScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerMeanScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerMomentsNumpyGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerMomentsNumpyGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerNearestNumbaGriddingPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerNearestNumbaGriddingPointCount.time_operation",
        "number": 1,
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerNearestNumbaGriddingPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestNumbaGriddingPointCount.track_end_to_end_time_s",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerNearestNumbaGriddingPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestNumbaGriddingPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerNearestNumbaGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerNearestNumbaGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerNearestNumbaGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestNumbaGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerNearestNumbaGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestNumbaGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerNearestRasterioReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerNearestRasterioReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerNearestRasterioReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestRasterioReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerNearestScipyGriddingPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerNearestScipyGriddingPointCount.time_operation",
        "number": 1,
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerNearestScipyGriddingPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestScipyGriddingPointCount.track_end_to_end_time_s",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerNearestScipyGriddingPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestScipyGriddingPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerNearestScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerNearestScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerNearestScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerRasterioPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerRasterioPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerRasterioPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerRasterioPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerRasterioPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerRasterioPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerRasterioRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerRasterioRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerRasterioRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerRasterioRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.EagerRobustNumpyGroupedStatsRobustSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.EagerRobustNumpyGroupedStatsRobustSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.EagerRobustNumpyGroupedStatsRobustSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerRobustNumpyGroupedStatsRobustSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.EagerRobustNumpyGroupedStatsRobustSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.EagerRobustNumpyGroupedStatsRobustSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.FloxDaskMomentsGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.FloxDaskMomentsGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.FloxDaskMomentsGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxDaskMomentsGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.FloxDaskMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxDaskMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.FloxDaskMomentsGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.FloxDaskMomentsGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.FloxDaskMomentsGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxDaskMomentsGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.FloxDaskMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxDaskMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.FloxEagerMomentsGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.FloxEagerMomentsGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.FloxEagerMomentsGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxEagerMomentsGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.FloxEagerMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxEagerMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.FloxEagerMomentsGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.FloxEagerMomentsGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.FloxEagerMomentsGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxEagerMomentsGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.FloxEagerMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.FloxEagerMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.GdalCliIdwGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliIdwGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliIdwGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliIdwGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliIdwGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliIdwGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.GdalCliLinearGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliLinearGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliLinearGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliLinearGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliLinearGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliLinearGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.GdalCliMeanGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliMeanGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliMeanGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliMeanGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliMeanGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliMeanGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.GdalCliNearestGriddingPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliNearestGriddingPointCount.time_operation",
        "number": 1,
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliNearestGriddingPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliNearestGriddingPointCount.track_end_to_end_time_s",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliNearestGriddingPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliNearestGriddingPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.GdalCliNearestGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliNearestGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliNearestGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliNearestGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliNearestGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliNearestGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.GdalCliNearestReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliNearestReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliNearestReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliNearestReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliNearestReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliNearestReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.GdalCliPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.GdalCliRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.GdalCliRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.GdalCliRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.GdalCliRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.GdalCliRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingIdwNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingIdwNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingIdwNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingIdwNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingIdwScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingIdwScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingIdwScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingIdwScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingLinearScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingLinearScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingLinearScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingLinearScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingLinearScipyInterpolationPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingLinearScipyInterpolationPointCount.time_operation",
        "number": 1,
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingLinearScipyInterpolationPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingLinearScipyInterpolationPointCount.track_end_to_end_time_s",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingMeanNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingMeanNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingMeanNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMeanNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingMeanScipyFilterChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingMeanScipyFilterChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingMeanScipyFilterChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMeanScipyFilterChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingMeanScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingMeanScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingMeanScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMeanScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingNearestNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingNearestNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingNearestNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingNearestNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingNearestRasterioReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingNearestRasterioReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingNearestRasterioReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingNearestRasterioReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingNearestScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingNearestScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingNearestScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingNearestScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingRasterioRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingRasterioRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingRasterioRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingRasterioRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.comparisons.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.comparisons.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "6e7ad649baece7a8061d1cfeceb44222eb1ce3886cabda4962389f4d5c2976ae",
        "warmup_time": 0
    },
    "asv_suite.comparisons.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, GdalRunner)\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "71ef40c18042e05f40dc0f48b1ff474127c3990a89f134c4b3ced47f096982f3"
    },
    "asv_suite.comparisons.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, GdalRunner):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference is not None:\n            operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner = GdalRunner(operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.comparisons.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "75dcf12e0b141624821f1a87ecc6b0d0250880de5312044e3ee6e99890f73b65"
    },
    "asv_suite.global_stats.GlobalDaskReduction.time_reduction": {
        "code": "class GlobalDaskReduction:\n    def time_reduction(self, implementation: Literal[\"geoutils\", \"native_dask\"], raster_size: int) -> None:\n        \"\"\"Compute the requested global estimates with the selected Dask reduction implementation.\"\"\"\n    \n        import dask\n    \n        del raster_size\n        with dask.config.set(scheduler=\"threads\", num_workers=1):\n            if implementation == \"geoutils\":\n                _reduce_values([self.values], self.statistics)\n            else:\n                estimates, count = _statistics_dask(self.values, self.aliases)\n                dask.compute(estimates, count)\n\n    def setup(self, implementation: Literal[\"geoutils\", \"native_dask\"], raster_size: int) -> None:\n        \"\"\"Prepare one finite Dask raster and the same mergeable statistic request for both implementations.\"\"\"\n    \n        import_optional(\"dask\", extra_name=\"benchmark\")\n        import dask.array as da\n    \n        del implementation\n        generator = np.random.default_rng(42)\n        values = generator.normal(size=(raster_size, raster_size))\n        values[::97, ::89] = np.nan\n        self.values = da.from_array(values, chunks=(256, 256))\n        self.aliases = {\"mean\", \"min\", \"max\", \"sum\", \"sumofsquares\", \"rmse\", \"std\"}\n        self.statistics = _normalize_statistics(sorted(self.aliases), grouped=False)",
        "min_run_count": 2,
        "name": "asv_suite.global_stats.GlobalDaskReduction.time_reduction",
        "number": 1,
        "param_names": [
            "implementation",
            "raster_size"
        ],
        "params": [
            [
                "'geoutils'",
                "'native_dask'"
            ],
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "cdc8e3a2892e9027c362e03bb4bd226285abe83e348894e9403e48f9bb81bb39",
        "warmup_time": 0
    },
    "asv_suite.operations.OperationBenchmarks.time_operation": {
        "code": "class OperationBenchmarks:\n    def time_operation(self, case: str) -> None:\n        \"\"\"Measure elapsed time through the operation's final output computation.\"\"\"\n    \n        # The time_ prefix tells ASV to time this method automatically\n        # Every large output is written before the measured method returns\n        self.runner._execute(self.operation)\n\n    def setup(self, case: str) -> None:\n        \"\"\"Prepare deterministic files and one backend outside the measured region.\"\"\"\n    \n        # One case identifier avoids the invalid product of all execution modes and operations\n        execution_mode, self.operation = split_operation_case(case)\n        raster_size = 512 if asv_pr_check_enabled() else 2048\n        self.runner = BenchmarkRunner(\n            execution_mode,\n            BenchmarkConfig(\n                shape=(raster_size, raster_size),\n                chunks=(512, 512),\n                memory_limit=\"1GB\",\n                subsample_size=2048,\n                ninterp=2048,\n                profile_interval=0.05,\n            ),\n        )\n    \n        # Source creation and worker startup do not belong to operation measurements\n        self.runner.start()",
        "min_run_count": 2,
        "name": "asv_suite.operations.OperationBenchmarks.time_operation",
        "number": 1,
        "param_names": [
            "case"
        ],
        "params": [
            [
                "'dask-crop'",
                "'dask-translate'",
                "'dask-copy'",
                "'dask-statistics'",
                "'dask-subsample'",
                "'multiprocessing-subsample'",
                "'dask-write'",
                "'dask-create_mask'",
                "'multiprocessing-create_mask'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "80c66bf0f767060cb1c4eb32d9c5d8a6c12808c63950ec12021dba28852236d1",
        "warmup_time": 0
    },
    "asv_suite.operations.OperationBenchmarks.track_peak_process_tree_mem_mb": {
        "code": "class OperationBenchmarks:\n    def track_peak_process_tree_mem_mb(self, case: str) -> float:\n        \"\"\"Measure aggregate peak RAM for the client and all backend processes.\"\"\"\n    \n        # The track_ prefix tells ASV to record the returned numeric measurement\n        # Profiling repeats the same complete operation with process-tree sampling enabled\n        result = self.runner.run(self.operation)\n        return result.peak_process_tree_mem_mb\n\n    def setup(self, case: str) -> None:\n        \"\"\"Prepare deterministic files and one backend outside the measured region.\"\"\"\n    \n        # One case identifier avoids the invalid product of all execution modes and operations\n        execution_mode, self.operation = split_operation_case(case)\n        raster_size = 512 if asv_pr_check_enabled() else 2048\n        self.runner = BenchmarkRunner(\n            execution_mode,\n            BenchmarkConfig(\n                shape=(raster_size, raster_size),\n                chunks=(512, 512),\n                memory_limit=\"1GB\",\n                subsample_size=2048,\n                ninterp=2048,\n                profile_interval=0.05,\n            ),\n        )\n    \n        # Source creation and worker startup do not belong to operation measurements\n        self.runner.start()",
        "name": "asv_suite.operations.OperationBenchmarks.track_peak_process_tree_mem_mb",
        "param_names": [
            "case"
        ],
        "params": [
            [
                "'dask-crop'",
                "'dask-translate'",
                "'dask-copy'",
                "'dask-statistics'",
                "'dask-subsample'",
                "'multiprocessing-subsample'",
                "'dask-write'",
                "'dask-create_mask'",
                "'multiprocessing-create_mask'"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "5ef6af445cba6803b7ee21468c0300f0576e905c02cda082334e015c60eb676b"
    },
    "asv_suite.variography.PointPairSamplingSize.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass PointPairSamplingSize:\n    def setup(self, n_points: int, strategy: str) -> None:\n        \"\"\"Prepare a constant-density point cloud and enough nearby candidates for each search method.\"\"\"\n    \n        self.source = prepare_pair_pointcloud(n_points)\n        self.pair_kwargs = {\n            \"n_pairs\": 200 if asv_pr_check_enabled() else 2_000,\n            \"strategy\": strategy,\n            \"min_distance\": 1,\n            \"max_distance\": n_points**0.5 / 2,\n            \"anchors_per_round\": 2_000,\n            \"nn_tolerance\": 0.5,\n            \"random_state\": 42,\n        }",
        "min_run_count": 2,
        "name": "asv_suite.variography.PointPairSamplingSize.time_operation",
        "number": 1,
        "param_names": [
            "n_points",
            "strategy"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'kdtree'",
                "'hashgrid'",
                "'nn_logvector'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "bb2f0711e308c19b2b1efecbcd4a1651114aa1810bb92535bea0814b8f7baac1",
        "warmup_time": 0
    },
    "asv_suite.variography.PointPairSamplingSize.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass PointPairSamplingSize:\n    def setup(self, n_points: int, strategy: str) -> None:\n        \"\"\"Prepare a constant-density point cloud and enough nearby candidates for each search method.\"\"\"\n    \n        self.source = prepare_pair_pointcloud(n_points)\n        self.pair_kwargs = {\n            \"n_pairs\": 200 if asv_pr_check_enabled() else 2_000,\n            \"strategy\": strategy,\n            \"min_distance\": 1,\n            \"max_distance\": n_points**0.5 / 2,\n            \"anchors_per_round\": 2_000,\n            \"nn_tolerance\": 0.5,\n            \"random_state\": 42,\n        }",
        "name": "asv_suite.variography.PointPairSamplingSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_points",
            "strategy"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'kdtree'",
                "'hashgrid'",
                "'nn_logvector'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "e92717c54cad39b110c99c48bcffcf7b52c2a9a16a4a84281112fcfdb264f230"
    },
    "asv_suite.variography.RasterPairSampling.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass RasterPairSampling:\n    def setup(self, n_pairs: int, execution_mode: Literal[\"eager\", \"dask\"], sampling_method: str) -> None:\n        \"\"\"Prepare one raster and fix batching, distance limits and the random seed for every method.\"\"\"\n    \n        size = 256 if asv_pr_check_enabled() else 1024\n        self._prepare(size, n_pairs, execution_mode, sampling_method)",
        "min_run_count": 2,
        "name": "asv_suite.variography.RasterPairSampling.time_operation",
        "number": 1,
        "param_names": [
            "n_pairs",
            "execution_mode",
            "sampling_method"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'eager'",
                "'dask'"
            ],
            [
                "'independent'",
                "'anchors'",
                "'chunk_anchors'",
                "'anchor_batched'",
                "'random_xy'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "fb94e1aba24011ca2946b6e43ea8e0b660a811e74bf9a2af9dabdff793e60ffd",
        "warmup_time": 0
    },
    "asv_suite.variography.RasterPairSampling.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass RasterPairSampling:\n    def setup(self, n_pairs: int, execution_mode: Literal[\"eager\", \"dask\"], sampling_method: str) -> None:\n        \"\"\"Prepare one raster and fix batching, distance limits and the random seed for every method.\"\"\"\n    \n        size = 256 if asv_pr_check_enabled() else 1024\n        self._prepare(size, n_pairs, execution_mode, sampling_method)",
        "name": "asv_suite.variography.RasterPairSampling.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_pairs",
            "execution_mode",
            "sampling_method"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'eager'",
                "'dask'"
            ],
            [
                "'independent'",
                "'anchors'",
                "'chunk_anchors'",
                "'anchor_batched'",
                "'random_xy'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "b0b76378014ab3d657af8303f4b6f409032c813d337a35e8fffaa90d17edce03"
    },
    "asv_suite.variography.RasterPairSamplingSize.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass RasterPairSamplingSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Keep 10,000 requested pairs while increasing the number of source chunks.\"\"\"\n    \n        n_pairs = 1_000 if asv_pr_check_enabled() else 10_000\n        self._prepare(raster_size, n_pairs, execution_mode, \"chunk_anchors\")",
        "min_run_count": 2,
        "name": "asv_suite.variography.RasterPairSamplingSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "c33e605f2e4ba6f81ab8bb319b0d9d048e8ea3a26e1186a89093bbee7e5ab7ba",
        "warmup_time": 0
    },
    "asv_suite.variography.RasterPairSamplingSize.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass RasterPairSamplingSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Keep 10,000 requested pairs while increasing the number of source chunks.\"\"\"\n    \n        n_pairs = 1_000 if asv_pr_check_enabled() else 10_000\n        self._prepare(raster_size, n_pairs, execution_mode, \"chunk_anchors\")",
        "name": "asv_suite.variography.RasterPairSamplingSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "32d904fc492a4b375c8030b4166b8cbf00f2a323d95494c91f14c03f6f1cab2f"
    },
    "asv_suite.variography.RasterVariogramSize.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass RasterVariogramSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Prepare the raster and estimator while leaving sampling and reduction inside timing.\"\"\"\n    \n        super().setup(raster_size, execution_mode)\n        _prepare_estimator(\"dowd\")",
        "min_run_count": 2,
        "name": "asv_suite.variography.RasterVariogramSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "db2629e6819e8deab0ac0e584dbb7cbbfbc5ab9e3da3320169f8fb6154bb643c",
        "warmup_time": 0
    },
    "asv_suite.variography.RasterVariogramSize.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass RasterVariogramSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Prepare the raster and estimator while leaving sampling and reduction inside timing.\"\"\"\n    \n        super().setup(raster_size, execution_mode)\n        _prepare_estimator(\"dowd\")",
        "name": "asv_suite.variography.RasterVariogramSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "5b752ff01dbc054b2f8666347e728d71a63dcd20a1d500d7a6459c44a7ad3023"
    },
    "asv_suite.variography.VariogramLagCount.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass VariogramLagCount:\n    def setup(self, n_lags: int, estimator: str) -> None:\n        \"\"\"Keep the pair sample fixed while changing only its number of distance bins.\"\"\"\n    \n        super().setup(1_000 if asv_pr_check_enabled() else 100_000, estimator)\n        self.n_lags = n_lags",
        "min_run_count": 2,
        "name": "asv_suite.variography.VariogramLagCount.time_operation",
        "number": 1,
        "param_names": [
            "n_lags",
            "estimator"
        ],
        "params": [
            [
                "24",
                "100",
                "256"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "ff933f3f4619fa27cdd17360bca2fe1e92621ff78786080cb8c4af0a3f9ae86f",
        "warmup_time": 0
    },
    "asv_suite.variography.VariogramLagCount.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass VariogramLagCount:\n    def setup(self, n_lags: int, estimator: str) -> None:\n        \"\"\"Keep the pair sample fixed while changing only its number of distance bins.\"\"\"\n    \n        super().setup(1_000 if asv_pr_check_enabled() else 100_000, estimator)\n        self.n_lags = n_lags",
        "name": "asv_suite.variography.VariogramLagCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_lags",
            "estimator"
        ],
        "params": [
            [
                "24",
                "100",
                "256"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "68cb99a6fa823c9ca47ac43a07f00c19df81687b9eb0f5f5949371f2f1fe9313"
    },
    "asv_suite.variography.VariogramPairCount.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass VariogramPairCount:\n    def setup(self, n_pairs: int, estimator: str) -> None:\n        \"\"\"Prepare the same finite pairs for both estimators outside the measured call.\"\"\"\n    \n        _prepare_estimator(estimator)\n        self.pairs = prepare_variogram_pairs(n_pairs)\n        self.estimator = estimator\n        self.n_lags = 24",
        "min_run_count": 2,
        "name": "asv_suite.variography.VariogramPairCount.time_operation",
        "number": 1,
        "param_names": [
            "n_pairs",
            "estimator"
        ],
        "params": [
            [
                "10000",
                "100000",
                "1000000"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "b0d78891e3ba7b000aab3571a62cbc790d2daa8c8674fde47a3531ae2e78c018",
        "warmup_time": 0
    },
    "asv_suite.variography.VariogramPairCount.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass VariogramPairCount:\n    def setup(self, n_pairs: int, estimator: str) -> None:\n        \"\"\"Prepare the same finite pairs for both estimators outside the measured call.\"\"\"\n    \n        _prepare_estimator(estimator)\n        self.pairs = prepare_variogram_pairs(n_pairs)\n        self.estimator = estimator\n        self.n_lags = 24",
        "name": "asv_suite.variography.VariogramPairCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_pairs",
            "estimator"
        ],
        "params": [
            [
                "10000",
                "100000",
                "1000000"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "1c097e48663cd37b8aa55c0010336525eb523e9eb8c704b6f589ab58eb27ab10"
    },
    "version": 2
}