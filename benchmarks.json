{
    "asv_suite.global_stats.GlobalDaskReduction.time_reduction": {
        "code": "class GlobalDaskReduction:\n    def time_reduction(self, implementation: Literal[\"geoutils\", \"native_dask\"], raster_size: int) -> None:\n        \"\"\"Compute the requested global estimates with the selected Dask reduction implementation.\"\"\"\n    \n        import dask\n    \n        del raster_size\n        with dask.config.set(scheduler=\"threads\", num_workers=1):\n            if implementation == \"geoutils\":\n                _reduce_values([self.values], self.statistics)\n            else:\n                estimates, count = _statistics_dask(self.values, self.aliases)\n                dask.compute(estimates, count)\n\n    def setup(self, implementation: Literal[\"geoutils\", \"native_dask\"], raster_size: int) -> None:\n        \"\"\"Prepare one finite Dask raster and the same mergeable statistic request for both implementations.\"\"\"\n    \n        import_optional(\"dask\", extra_name=\"benchmark\")\n        import dask.array as da\n    \n        del implementation\n        generator = np.random.default_rng(42)\n        values = generator.normal(size=(raster_size, raster_size))\n        values[::97, ::89] = np.nan\n        self.values = da.from_array(values, chunks=(256, 256))\n        self.aliases = {\"mean\", \"min\", \"max\", \"sum\", \"sumofsquares\", \"rmse\", \"std\"}\n        self.statistics = _normalize_statistics(sorted(self.aliases), grouped=False)",
        "min_run_count": 2,
        "name": "asv_suite.global_stats.GlobalDaskReduction.time_reduction",
        "number": 1,
        "param_names": [
            "implementation",
            "raster_size"
        ],
        "params": [
            [
                "'geoutils'",
                "'native_dask'"
            ],
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "cdc8e3a2892e9027c362e03bb4bd226285abe83e348894e9403e48f9bb81bb39",
        "warmup_time": 0
    },
    "asv_suite.operations.OperationBenchmarks.time_operation": {
        "code": "class OperationBenchmarks:\n    def time_operation(self, case: str) -> None:\n        \"\"\"Measure elapsed time through the operation's final output computation.\"\"\"\n    \n        # The time_ prefix tells ASV to time this method automatically\n        # Every large output is written before the measured method returns\n        self.runner._execute(self.operation)\n\n    def setup(self, case: str) -> None:\n        \"\"\"Prepare deterministic files and one backend outside the measured region.\"\"\"\n    \n        # One case identifier avoids the invalid product of all execution modes and operations\n        execution_mode, self.operation = split_operation_case(case)\n        raster_size = 512 if asv_pr_check_enabled() else 2048\n        self.runner = BenchmarkRunner(\n            execution_mode,\n            BenchmarkConfig(\n                shape=(raster_size, raster_size),\n                chunks=(512, 512),\n                memory_limit=\"1GB\",\n                subsample_size=2048,\n                ninterp=2048,\n                profile_interval=0.05,\n            ),\n        )\n    \n        # Source creation and worker startup do not belong to operation measurements\n        self.runner.start()",
        "min_run_count": 2,
        "name": "asv_suite.operations.OperationBenchmarks.time_operation",
        "number": 1,
        "param_names": [
            "case"
        ],
        "params": [
            [
                "'dask-crop'",
                "'dask-translate'",
                "'dask-copy'",
                "'dask-statistics'",
                "'dask-write'",
                "'dask-create_mask'",
                "'multiprocessing-create_mask'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "80c66bf0f767060cb1c4eb32d9c5d8a6c12808c63950ec12021dba28852236d1",
        "warmup_time": 0
    },
    "asv_suite.operations.OperationBenchmarks.track_peak_process_tree_mem_mb": {
        "code": "class OperationBenchmarks:\n    def track_peak_process_tree_mem_mb(self, case: str) -> float:\n        \"\"\"Measure aggregate peak RAM for the client and all backend processes.\"\"\"\n    \n        # The track_ prefix tells ASV to record the returned numeric measurement\n        # Profiling repeats the same complete operation with process-tree sampling enabled\n        result = self.runner.run(self.operation)\n        return result.peak_process_tree_mem_mb\n\n    def setup(self, case: str) -> None:\n        \"\"\"Prepare deterministic files and one backend outside the measured region.\"\"\"\n    \n        # One case identifier avoids the invalid product of all execution modes and operations\n        execution_mode, self.operation = split_operation_case(case)\n        raster_size = 512 if asv_pr_check_enabled() else 2048\n        self.runner = BenchmarkRunner(\n            execution_mode,\n            BenchmarkConfig(\n                shape=(raster_size, raster_size),\n                chunks=(512, 512),\n                memory_limit=\"1GB\",\n                subsample_size=2048,\n                ninterp=2048,\n                profile_interval=0.05,\n            ),\n        )\n    \n        # Source creation and worker startup do not belong to operation measurements\n        self.runner.start()",
        "name": "asv_suite.operations.OperationBenchmarks.track_peak_process_tree_mem_mb",
        "param_names": [
            "case"
        ],
        "params": [
            [
                "'dask-crop'",
                "'dask-translate'",
                "'dask-copy'",
                "'dask-statistics'",
                "'dask-write'",
                "'dask-create_mask'",
                "'multiprocessing-create_mask'"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "5ef6af445cba6803b7ee21468c0300f0576e905c02cda082334e015c60eb676b"
    },
    "asv_suite.parameter_sweeps.DaskIdwNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskIdwNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskIdwNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskIdwNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskIdwScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskIdwScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskIdwScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskIdwScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskLinearScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskLinearScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskLinearScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskLinearScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskLinearScipyInterpolationPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskLinearScipyInterpolationPointCount.time_operation",
        "number": 1,
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskLinearScipyInterpolationPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskLinearScipyInterpolationPointCount.track_end_to_end_time_s",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMeanNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMeanNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMeanNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMeanNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMeanScipyFilterChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMeanScipyFilterChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMeanScipyFilterChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMeanScipyFilterChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMeanScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMeanScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMeanScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMeanScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyAutoGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpyGroupwiseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsGroupCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsGroupCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsInterleavedChunks.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "64",
                "193",
                "512"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskMomentsNumpySparseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskNearestNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskNearestNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskNearestNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskNearestNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskNearestRasterioReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskNearestRasterioReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskNearestRasterioReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskNearestRasterioReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskNearestScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskNearestScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskNearestScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskNearestScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskRasterioGeometryStitchPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskRasterioGeometryStitchPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskRasterioGeometryStitchPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioGeometryStitchPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskRasterioGeometryStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioGeometryStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskRasterioLabelStitchPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskRasterioLabelStitchPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskRasterioLabelUnionPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskRasterioLabelUnionPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskRasterioLabelUnionPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioLabelUnionPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskRasterioLabelUnionPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioLabelUnionPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskRasterioRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskRasterioRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskRasterioRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskSubsampleSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskSubsampleSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskSubsampleSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskSubsampleSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskSubsampleSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskSubsampleSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.DaskToPointcloudRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.DaskToPointcloudRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.DaskToPointcloudRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskToPointcloudRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.DaskToPointcloudRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.DaskToPointcloudRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerIdwNumbaGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerIdwNumbaGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerIdwNumbaGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerIdwNumbaGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerIdwNumbaGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerIdwNumbaGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerIdwScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerIdwScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerIdwScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerIdwScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerLinearScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerLinearScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerLinearScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerLinearScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerLinearScipyInterpolationPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerLinearScipyInterpolationPointCount.time_operation",
        "number": 1,
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerLinearScipyInterpolationPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerLinearScipyInterpolationPointCount.track_end_to_end_time_s",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerMeanNumbaGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerMeanNumbaGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerMeanNumbaGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerMeanNumbaGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerMeanNumbaGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerMeanNumbaGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerMeanScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerMeanScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerMeanScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerMeanScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerMomentsNumpyGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingPointCount.time_operation",
        "number": 1,
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingPointCount.track_end_to_end_time_s",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestNumbaGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerNearestRasterioReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerNearestRasterioReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerNearestRasterioReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestRasterioReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerNearestScipyGriddingPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerNearestScipyGriddingPointCount.time_operation",
        "number": 1,
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerNearestScipyGriddingPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestScipyGriddingPointCount.track_end_to_end_time_s",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerNearestScipyGriddingPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestScipyGriddingPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerNearestScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerNearestScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerNearestScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerRasterioPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerRasterioPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerRasterioPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerRasterioPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerRasterioPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerRasterioPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerRasterioRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerRasterioRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerRasterioRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerRasterioRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.EagerRobustNumpyGroupedStatsRobustSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.EagerRobustNumpyGroupedStatsRobustSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.EagerRobustNumpyGroupedStatsRobustSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerRobustNumpyGroupedStatsRobustSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.EagerRobustNumpyGroupedStatsRobustSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.EagerRobustNumpyGroupedStatsRobustSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxDaskMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.FloxEagerMomentsGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.GdalCliIdwGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliIdwGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliIdwGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliIdwGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliIdwGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliIdwGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.GdalCliLinearGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliLinearGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliLinearGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliLinearGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliLinearGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliLinearGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.GdalCliMeanGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliMeanGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliMeanGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliMeanGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliMeanGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliMeanGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.GdalCliNearestGriddingPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliNearestGriddingPointCount.time_operation",
        "number": 1,
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliNearestGriddingPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliNearestGriddingPointCount.track_end_to_end_time_s",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliNearestGriddingPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliNearestGriddingPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "points_per_axis"
        ],
        "params": [
            [
                "3",
                "9",
                "33"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.GdalCliNearestGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliNearestGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliNearestGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliNearestGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliNearestGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliNearestGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.GdalCliNearestReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliNearestReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliNearestReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliNearestReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliNearestReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliNearestReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.GdalCliPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.GdalCliRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.GdalCliRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.GdalCliRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.GdalCliRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.GdalCliRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingIdwNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingIdwNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingIdwNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingIdwNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingIdwNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingIdwScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingIdwScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingIdwScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingIdwScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingIdwScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLasSubsampleLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingLasSubsampleLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingLasSubsampleLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLasSubsampleLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLasSubsampleLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLasSubsampleLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLasToPointcloudLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingLasToPointcloudLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingLasToPointcloudLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLasToPointcloudLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLasToPointcloudLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLasToPointcloudLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLazSubsampleLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingLazSubsampleLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingLazSubsampleLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLazSubsampleLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLazSubsampleLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLazSubsampleLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLazToPointcloudLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingLazToPointcloudLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingLazToPointcloudLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLazToPointcloudLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLazToPointcloudLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLazToPointcloudLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLinearScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingLinearScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingLinearScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLinearScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLinearScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLinearScipyInterpolationPointCount.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingLinearScipyInterpolationPointCount.time_operation",
        "number": 1,
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingLinearScipyInterpolationPointCount.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLinearScipyInterpolationPointCount.track_end_to_end_time_s",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingLinearScipyInterpolationPointCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "interpolated_points"
        ],
        "params": [
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanScipyFilterChunkSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanScipyFilterChunkSize.time_operation",
        "number": 1,
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanScipyFilterChunkSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanScipyFilterChunkSize.track_end_to_end_time_s",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanScipyFilterChunkSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "chunk_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMeanScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.time_operation",
        "number": 1,
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_end_to_end_time_s",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxGroupCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "groups_per_axis"
        ],
        "params": [
            [
                "4",
                "16",
                "65"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.time_operation": {
        "code": "class _GroupedFloxRasterSize:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Compute every requested result with one threaded or multiprocessing worker.\"\"\"\n    \n        with self.dask.config.set(scheduler=\"threads\", num_workers=1):\n            compute_grouped_reference(*self.inputs, implementation=self.implementation, mp_config=self.mp_config)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "c0ffb87a70b9fb10508d9987bb1106ca96144d68820fb738b00ff9734f8e51af",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s": {
        "code": "class _GroupedFloxRasterSize:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure masking, grouping and complete output construction from prepared inputs.\"\"\"\n    \n        start = time.perf_counter()\n        self.time_operation(parameter)\n        return time.perf_counter() - start\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "5a725bf53186bb7a957b0bea40e5d793e3f2766345b018e4b6e6d302a0be0a32"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _GroupedFloxRasterSize:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak process memory while the complete grouped result is calculated.\"\"\"\n    \n        _, metrics = profile_call(self.time_operation, parameter, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare common arrays and load optional libraries outside the measurement.\"\"\"\n    \n        # Select the same execution mode for GeoUtils and its corresponding Flox reference\n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        selected = case or reference\n        assert selected is not None and selected.execution_mode is not None\n        self.implementation: Literal[\"geoutils\", \"flox\"] = \"geoutils\" if reference is None else \"flox\"\n        if reference is not None:\n            try:\n                import_optional(\"flox\", extra_name=\"benchmark\")\n            except ImportError as exc:\n                raise NotImplementedError(\"Install optional flox to run this comparison\") from exc\n    \n        # Fix the scheduler for both libraries and construct all observations before timing starts\n        self.dask = import_optional(\"dask\", extra_name=\"benchmark\")\n        config = self.make_config(parameter)\n    \n        # Start one persistent worker before building arrays so it receives only serialized tiles\n        self.mp_cluster: MpCluster | None = None\n        self.mp_config: MultiprocConfig | None = None\n        if selected.execution_mode == \"multiprocessing\":\n            self.mp_cluster = MpCluster({\"nb_workers\": 1, \"max_tasks_per_child\": None})\n            self.mp_config = MultiprocConfig(chunks=config.chunks, cluster=self.mp_cluster)\n    \n        # Keep complete prepared inputs in the client, with identical logical tiles for both worker backends\n        self.inputs = prepare_grouped_reference(\n            config.shape[0],\n            config.grouped_regions_per_axis,\n            config.grouped_layout,\n            selected.execution_mode,\n        )",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyAutoGroupedFloxRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1a1edd0daa0d318e5a2b6169d99d16850713eacc264f5dfa50cb84b83ce87fe4"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingMomentsNumpyDenseGroupedStatsRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestNumbaWorkerIntegration.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestNumbaWorkerIntegration.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestNumbaWorkerIntegration.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestNumbaWorkerIntegration.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestNumbaWorkerIntegration.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestRasterioReprojectionRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestRasterioReprojectionRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestRasterioReprojectionRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestRasterioReprojectionRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestRasterioReprojectionRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestScipyGriddingRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestScipyGriddingRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestScipyGriddingRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestScipyGriddingRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingNearestScipyGriddingRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingRasterioLabelStitchPolygonizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingRasterioRasterizationRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingRasterioRasterizationRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingRasterioRasterizationRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingRasterioRasterizationRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingRasterioRasterizationRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "1024",
                "2048",
                "4096"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingRobustNumpyGroupwiseGroupedStatsRobustSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "512",
                "1024",
                "2048"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingSubsampleSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingSubsampleSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingSubsampleSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingSubsampleSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingSubsampleSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingSubsampleSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.MultiprocessingToPointcloudRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.MultiprocessingToPointcloudRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.MultiprocessingToPointcloudRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingToPointcloudRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.MultiprocessingToPointcloudRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.MultiprocessingToPointcloudRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.PdalCliLasSubsampleLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.PdalCliLasSubsampleLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.PdalCliLasSubsampleLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLasSubsampleLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.PdalCliLasSubsampleLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLasSubsampleLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.PdalCliLasToPointcloudLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.PdalCliLasToPointcloudLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.PdalCliLasToPointcloudLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLasToPointcloudLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.PdalCliLasToPointcloudLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLasToPointcloudLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.PdalCliLazSubsampleLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.PdalCliLazSubsampleLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.PdalCliLazSubsampleLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLazSubsampleLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.PdalCliLazSubsampleLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLazSubsampleLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.PdalCliLazToPointcloudLasLazSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.PdalCliLazToPointcloudLasLazSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.PdalCliLazToPointcloudLasLazSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLazToPointcloudLasLazSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.PdalCliLazToPointcloudLasLazSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliLazToPointcloudLasLazSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.PdalCliSubsampleSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.PdalCliSubsampleSize.time_operation",
        "number": 1,
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.PdalCliSubsampleSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliSubsampleSize.track_end_to_end_time_s",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.PdalCliSubsampleSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliSubsampleSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "subsample_size"
        ],
        "params": [
            [
                "256",
                "16384",
                "524288"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.parameter_sweeps.PdalCliToPointcloudRasterSize.time_operation": {
        "code": "class _ComparisonBenchmark:\n    def time_operation(self, parameter: int) -> None:\n        \"\"\"Measure a complete operation after execution-mode initialization.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            self.runner._execute()\n        else:\n            self.runner._execute(self.operation)\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "min_run_count": 2,
        "name": "asv_suite.parameter_sweeps.PdalCliToPointcloudRasterSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "repeat": 2,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "fd532bcfac5b68d3ee548bbc551efac1d0e9cffeb148b6d5ec3db232a2c66625",
        "warmup_time": 0
    },
    "asv_suite.parameter_sweeps.PdalCliToPointcloudRasterSize.track_end_to_end_time_s": {
        "code": "class _ComparisonBenchmark:\n    def track_end_to_end_time_s(self, parameter: int) -> float:\n        \"\"\"Measure execution-mode initialization followed by one complete operation.\"\"\"\n    \n        if self.external_reference is not None:\n            start_time = time.perf_counter()\n            assert isinstance(self.runner, (GdalRunner, PdalRunner))\n            self.runner._execute()\n            return time.perf_counter() - start_time\n    \n        self.runner.close()\n        assert self.execution_mode is not None\n        fresh_runner = BenchmarkRunner(self.execution_mode, self.config)\n        start_time = time.perf_counter()\n        try:\n            fresh_runner.start()\n            fresh_runner._execute(self.operation)\n        finally:\n            elapsed_time_s = time.perf_counter() - start_time\n            fresh_runner.close()\n        self.runner = fresh_runner\n        return elapsed_time_s\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliToPointcloudRasterSize.track_end_to_end_time_s",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "seconds",
        "version": "336e019641895f2a4f5bb4a52981a305d43f63040c0a8a442a777658e13358a6"
    },
    "asv_suite.parameter_sweeps.PdalCliToPointcloudRasterSize.track_peak_process_tree_mem_mb": {
        "code": "class _ComparisonBenchmark:\n    def track_peak_process_tree_mem_mb(self, parameter: int) -> float:\n        \"\"\"Measure peak memory for the benchmark process and execution-mode children.\"\"\"\n    \n        if isinstance(self.runner, (GdalRunner, PdalRunner)):\n            return self.runner.run().peak_process_tree_mem_mb\n        return self.runner.run(self.operation).peak_process_tree_mem_mb\n\n    def setup(self, parameter: int) -> None:\n        \"\"\"Prepare deterministic files and initialize one execution case.\"\"\"\n    \n        benchmark_class = type(self).__name__\n        case = BENCHMARK_CASE_BY_CLASS.get(benchmark_class)\n        reference_case = EXTERNAL_REFERENCE_CASE_BY_CLASS.get(benchmark_class)\n        if (case is None) == (reference_case is None):\n            raise ValueError(f\"Expected exactly one registered benchmark case for {benchmark_class}\")\n    \n        selected_case = case or reference_case\n        assert selected_case is not None\n        if asv_pr_check_enabled() and not selected_case.pr_check:\n            raise NotImplementedError(\"Benchmark case omitted from the pull-request sample\")\n    \n        # Strategies only identify how Dask or multiprocessing coordinates chunks\n        self.operation = selected_case.operation\n        self.operation_method = selected_case.method\n        self.operation_strategy = selected_case.strategy\n        self.calculation_engine = case.calculation_engine if case is not None else None\n        self.execution_mode = case.execution_mode if case is not None else None\n        self.external_reference = reference_case.external_reference if reference_case is not None else None\n    \n        # Input generation remains outside all three measured boundaries\n        self._tmpdir = tempfile.TemporaryDirectory(prefix=\"geoutils-asv-comparison-\")\n        self.config = self.make_config(parameter)\n        self.config.point_output_driver = selected_case.output_driver\n        self.config.operation_method = self.operation_method\n        self.config.calculation_engine = self.calculation_engine\n        self.config.operation_strategy = self.operation_strategy\n        self.config.directory = self._tmpdir.name\n        self.sources = BenchmarkRunner(\"eager\", self.config).prepare_sources()\n    \n        if self.external_reference == \"gdal_cli\":\n            gdal_operation = cast(ComparisonOperation, self.operation)\n            self.runner: BenchmarkRunner | GdalRunner | PdalRunner = GdalRunner(\n                gdal_operation, self.config, self.sources\n            )\n        elif self.external_reference == \"pdal_cli\":\n            pdal_operation = cast(PdalComparisonOperation, self.operation)\n            self.runner = PdalRunner(pdal_operation, self.config, self.sources)\n        else:\n            assert self.execution_mode is not None\n            self.runner = BenchmarkRunner(self.execution_mode, self.config).start()",
        "name": "asv_suite.parameter_sweeps.PdalCliToPointcloudRasterSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size"
        ],
        "params": [
            [
                "256",
                "512",
                "1024"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "df16da928734a9ada96dbd608593a226a6bc4427dd6374ac11830dac7cfa9f6d"
    },
    "asv_suite.subsampling.DaskCutoffComparison.time_cutoff": {
        "code": "class DaskCutoffComparison:\n    def time_cutoff(self, implementation: Literal[\"geoutils_cutoff\", \"dask_topk\"], subsample_size: int) -> None:\n        \"\"\"Measure exact selection above the largest raster chunk.\"\"\"\n    \n        self._run(implementation, subsample_size)\n\n    def setup(self, implementation: Literal[\"geoutils_cutoff\", \"dask_topk\"], subsample_size: int) -> None:\n        \"\"\"Prepare equivalent lazy keys and raster chunks for both selection methods.\"\"\"\n    \n        import_optional(\"dask\", extra_name=\"benchmark\")\n        import dask.array as da\n    \n        shape = (2048, 2048)\n        rows = da.arange(shape[0], chunks=512)[:, None]\n        columns = da.arange(shape[1], chunks=512)[None, :]\n        positions = rows * shape[1] + columns\n        self.values = da.where(positions % 19 == 0, np.nan, 1.0).astype(np.float32)\n        self.shape = shape\n    \n        row_chunks, column_chunks = self.values.chunks\n        row_starts = np.cumsum((0, *row_chunks))\n        column_starts = np.cumsum((0, *column_chunks))\n        tiles = np.array(\n            [\n                (row_starts[row], row_starts[row + 1], column_starts[column], column_starts[column + 1])\n                for row in range(len(row_chunks))\n                for column in range(len(column_chunks))\n            ],\n            dtype=np.int64,\n        )\n        self.blocks = self.values.to_delayed().ravel().tolist()\n        self.block_ids = [\n            {\n                \"row_start\": int(tile[0]),\n                \"row_stop\": int(tile[1]),\n                \"col_start\": int(tile[2]),\n                \"col_stop\": int(tile[3]),\n            }\n            for tile in tiles\n        ]\n        self.largest_chunk = max(int(rows * columns) for rows in row_chunks for columns in column_chunks)",
        "min_run_count": 2,
        "name": "asv_suite.subsampling.DaskCutoffComparison.time_cutoff",
        "number": 1,
        "param_names": [
            "implementation",
            "subsample_size"
        ],
        "params": [
            [
                "'geoutils_cutoff'",
                "'dask_topk'"
            ],
            [
                "262145",
                "524288",
                "1048576"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "3b30ed8cdbed33c3d41b8f72e4330736208ab5b129ba414a6882109bdd9d0385",
        "warmup_time": 0
    },
    "asv_suite.subsampling.DaskCutoffComparison.track_peak_client_mem_mb": {
        "code": "class DaskCutoffComparison:\n    def track_peak_client_mem_mb(\n        self, implementation: Literal[\"geoutils_cutoff\", \"dask_topk\"], subsample_size: int\n    ) -> float:\n        \"\"\"Measure peak client memory while finding the exact selection boundary.\"\"\"\n    \n        _, metrics = profile_call(self._run, implementation, subsample_size, dask=False, include_children=False)\n        return metrics.peak_client_mem_mb\n\n    def setup(self, implementation: Literal[\"geoutils_cutoff\", \"dask_topk\"], subsample_size: int) -> None:\n        \"\"\"Prepare equivalent lazy keys and raster chunks for both selection methods.\"\"\"\n    \n        import_optional(\"dask\", extra_name=\"benchmark\")\n        import dask.array as da\n    \n        shape = (2048, 2048)\n        rows = da.arange(shape[0], chunks=512)[:, None]\n        columns = da.arange(shape[1], chunks=512)[None, :]\n        positions = rows * shape[1] + columns\n        self.values = da.where(positions % 19 == 0, np.nan, 1.0).astype(np.float32)\n        self.shape = shape\n    \n        row_chunks, column_chunks = self.values.chunks\n        row_starts = np.cumsum((0, *row_chunks))\n        column_starts = np.cumsum((0, *column_chunks))\n        tiles = np.array(\n            [\n                (row_starts[row], row_starts[row + 1], column_starts[column], column_starts[column + 1])\n                for row in range(len(row_chunks))\n                for column in range(len(column_chunks))\n            ],\n            dtype=np.int64,\n        )\n        self.blocks = self.values.to_delayed().ravel().tolist()\n        self.block_ids = [\n            {\n                \"row_start\": int(tile[0]),\n                \"row_stop\": int(tile[1]),\n                \"col_start\": int(tile[2]),\n                \"col_stop\": int(tile[3]),\n            }\n            for tile in tiles\n        ]\n        self.largest_chunk = max(int(rows * columns) for rows in row_chunks for columns in column_chunks)",
        "name": "asv_suite.subsampling.DaskCutoffComparison.track_peak_client_mem_mb",
        "param_names": [
            "implementation",
            "subsample_size"
        ],
        "params": [
            [
                "'geoutils_cutoff'",
                "'dask_topk'"
            ],
            [
                "262145",
                "524288",
                "1048576"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "1097c4c39b01cbf936ccb4136f75be72380c705c27a91365e39f304ff963c864"
    },
    "asv_suite.subsampling.DaskTopkComparison.time_topk": {
        "code": "class DaskTopkComparison:\n    def time_topk(self, implementation: Literal[\"geoutils\", \"dask_argtopk\"], subsample_size: int) -> None:\n        \"\"\"Measure complete deterministic selection after preparing the lazy raster.\"\"\"\n    \n        self._run(implementation, subsample_size)\n\n    def setup(self, implementation: Literal[\"geoutils\", \"dask_argtopk\"], subsample_size: int) -> None:\n        \"\"\"Prepare one lazy raster containing regular nodata cells.\"\"\"\n    \n        import_optional(\"dask\", extra_name=\"benchmark\")\n        import dask.array as da\n    \n        shape = (2048, 2048)\n        rows = da.arange(shape[0], chunks=512)[:, None]\n        columns = da.arange(shape[1], chunks=512)[None, :]\n        positions = rows * shape[1] + columns\n        values = da.where(positions % 19 == 0, np.nan, 1.0).astype(np.float32)\n        self.raster = gu.RasterAccessor.from_array(values, from_origin(0, shape[0], 1, 1), 32633)",
        "min_run_count": 2,
        "name": "asv_suite.subsampling.DaskTopkComparison.time_topk",
        "number": 1,
        "param_names": [
            "implementation",
            "subsample_size"
        ],
        "params": [
            [
                "'geoutils'",
                "'dask_argtopk'"
            ],
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 900,
        "type": "time",
        "unit": "seconds",
        "version": "9dcf77aad3d0aa04aa889b513bab6000befffc58f4198d06c526fec0c8fca6d8",
        "warmup_time": 0
    },
    "asv_suite.subsampling.DaskTopkComparison.track_peak_client_mem_mb": {
        "code": "class DaskTopkComparison:\n    def track_peak_client_mem_mb(\n        self, implementation: Literal[\"geoutils\", \"dask_argtopk\"], subsample_size: int\n    ) -> float:\n        \"\"\"Measure peak client memory during deterministic selection.\"\"\"\n    \n        _, metrics = profile_call(self._run, implementation, subsample_size, dask=False, include_children=False)\n        return metrics.peak_client_mem_mb\n\n    def setup(self, implementation: Literal[\"geoutils\", \"dask_argtopk\"], subsample_size: int) -> None:\n        \"\"\"Prepare one lazy raster containing regular nodata cells.\"\"\"\n    \n        import_optional(\"dask\", extra_name=\"benchmark\")\n        import dask.array as da\n    \n        shape = (2048, 2048)\n        rows = da.arange(shape[0], chunks=512)[:, None]\n        columns = da.arange(shape[1], chunks=512)[None, :]\n        positions = rows * shape[1] + columns\n        values = da.where(positions % 19 == 0, np.nan, 1.0).astype(np.float32)\n        self.raster = gu.RasterAccessor.from_array(values, from_origin(0, shape[0], 1, 1), 32633)",
        "name": "asv_suite.subsampling.DaskTopkComparison.track_peak_client_mem_mb",
        "param_names": [
            "implementation",
            "subsample_size"
        ],
        "params": [
            [
                "'geoutils'",
                "'dask_argtopk'"
            ],
            [
                "256",
                "2048",
                "16384"
            ]
        ],
        "timeout": 900,
        "type": "track",
        "unit": "MB",
        "version": "ea1be7aac46824d38e58d6a60bbcc0ff1382532bf99d6d8ee5f2b8662a845a92"
    },
    "asv_suite.variography.PointPairSamplingSize.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass PointPairSamplingSize:\n    def setup(self, n_points: int, strategy: str) -> None:\n        \"\"\"Prepare a constant-density point cloud and enough nearby candidates for each search method.\"\"\"\n    \n        self.source = prepare_pair_pointcloud(n_points)\n        self.pair_kwargs = {\n            \"n_pairs\": 200 if asv_pr_check_enabled() else 2_000,\n            \"strategy\": strategy,\n            \"min_distance\": 1,\n            \"max_distance\": n_points**0.5 / 2,\n            \"anchors_per_round\": 2_000,\n            \"nn_tolerance\": 0.5,\n            \"random_state\": 42,\n        }",
        "min_run_count": 2,
        "name": "asv_suite.variography.PointPairSamplingSize.time_operation",
        "number": 1,
        "param_names": [
            "n_points",
            "strategy"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'kdtree'",
                "'hashgrid'",
                "'nn_logvector'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "bb2f0711e308c19b2b1efecbcd4a1651114aa1810bb92535bea0814b8f7baac1",
        "warmup_time": 0
    },
    "asv_suite.variography.PointPairSamplingSize.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass PointPairSamplingSize:\n    def setup(self, n_points: int, strategy: str) -> None:\n        \"\"\"Prepare a constant-density point cloud and enough nearby candidates for each search method.\"\"\"\n    \n        self.source = prepare_pair_pointcloud(n_points)\n        self.pair_kwargs = {\n            \"n_pairs\": 200 if asv_pr_check_enabled() else 2_000,\n            \"strategy\": strategy,\n            \"min_distance\": 1,\n            \"max_distance\": n_points**0.5 / 2,\n            \"anchors_per_round\": 2_000,\n            \"nn_tolerance\": 0.5,\n            \"random_state\": 42,\n        }",
        "name": "asv_suite.variography.PointPairSamplingSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_points",
            "strategy"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'kdtree'",
                "'hashgrid'",
                "'nn_logvector'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "e92717c54cad39b110c99c48bcffcf7b52c2a9a16a4a84281112fcfdb264f230"
    },
    "asv_suite.variography.RasterPairSampling.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass RasterPairSampling:\n    def setup(self, n_pairs: int, execution_mode: Literal[\"eager\", \"dask\"], sampling_method: str) -> None:\n        \"\"\"Prepare one raster and fix batching, distance limits and the random seed for every method.\"\"\"\n    \n        size = 256 if asv_pr_check_enabled() else 1024\n        self._prepare(size, n_pairs, execution_mode, sampling_method)",
        "min_run_count": 2,
        "name": "asv_suite.variography.RasterPairSampling.time_operation",
        "number": 1,
        "param_names": [
            "n_pairs",
            "execution_mode",
            "sampling_method"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'eager'",
                "'dask'"
            ],
            [
                "'independent'",
                "'anchors'",
                "'chunk_anchors'",
                "'anchor_batched'",
                "'random_xy'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "fb94e1aba24011ca2946b6e43ea8e0b660a811e74bf9a2af9dabdff793e60ffd",
        "warmup_time": 0
    },
    "asv_suite.variography.RasterPairSampling.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass RasterPairSampling:\n    def setup(self, n_pairs: int, execution_mode: Literal[\"eager\", \"dask\"], sampling_method: str) -> None:\n        \"\"\"Prepare one raster and fix batching, distance limits and the random seed for every method.\"\"\"\n    \n        size = 256 if asv_pr_check_enabled() else 1024\n        self._prepare(size, n_pairs, execution_mode, sampling_method)",
        "name": "asv_suite.variography.RasterPairSampling.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_pairs",
            "execution_mode",
            "sampling_method"
        ],
        "params": [
            [
                "1000",
                "10000",
                "100000"
            ],
            [
                "'eager'",
                "'dask'"
            ],
            [
                "'independent'",
                "'anchors'",
                "'chunk_anchors'",
                "'anchor_batched'",
                "'random_xy'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "b0b76378014ab3d657af8303f4b6f409032c813d337a35e8fffaa90d17edce03"
    },
    "asv_suite.variography.RasterPairSamplingSize.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass RasterPairSamplingSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Keep 10,000 requested pairs while increasing the number of source chunks.\"\"\"\n    \n        n_pairs = 1_000 if asv_pr_check_enabled() else 10_000\n        self._prepare(raster_size, n_pairs, execution_mode, \"chunk_anchors\")",
        "min_run_count": 2,
        "name": "asv_suite.variography.RasterPairSamplingSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "c33e605f2e4ba6f81ab8bb319b0d9d048e8ea3a26e1186a89093bbee7e5ab7ba",
        "warmup_time": 0
    },
    "asv_suite.variography.RasterPairSamplingSize.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass RasterPairSamplingSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Keep 10,000 requested pairs while increasing the number of source chunks.\"\"\"\n    \n        n_pairs = 1_000 if asv_pr_check_enabled() else 10_000\n        self._prepare(raster_size, n_pairs, execution_mode, \"chunk_anchors\")",
        "name": "asv_suite.variography.RasterPairSamplingSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "32d904fc492a4b375c8030b4166b8cbf00f2a323d95494c91f14c03f6f1cab2f"
    },
    "asv_suite.variography.RasterVariogramSize.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass RasterVariogramSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Prepare the raster and estimator while leaving sampling and reduction inside timing.\"\"\"\n    \n        super().setup(raster_size, execution_mode)\n        _prepare_estimator(\"dowd\")",
        "min_run_count": 2,
        "name": "asv_suite.variography.RasterVariogramSize.time_operation",
        "number": 1,
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "db2629e6819e8deab0ac0e584dbb7cbbfbc5ab9e3da3320169f8fb6154bb643c",
        "warmup_time": 0
    },
    "asv_suite.variography.RasterVariogramSize.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass RasterVariogramSize:\n    def setup(self, raster_size: int, execution_mode: Literal[\"eager\", \"dask\"]) -> None:\n        \"\"\"Prepare the raster and estimator while leaving sampling and reduction inside timing.\"\"\"\n    \n        super().setup(raster_size, execution_mode)\n        _prepare_estimator(\"dowd\")",
        "name": "asv_suite.variography.RasterVariogramSize.track_peak_process_tree_mem_mb",
        "param_names": [
            "raster_size",
            "execution_mode"
        ],
        "params": [
            [
                "256",
                "1024",
                "4096"
            ],
            [
                "'eager'",
                "'dask'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "5b752ff01dbc054b2f8666347e728d71a63dcd20a1d500d7a6459c44a7ad3023"
    },
    "asv_suite.variography.VariogramLagCount.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass VariogramLagCount:\n    def setup(self, n_lags: int, estimator: str) -> None:\n        \"\"\"Keep the pair sample fixed while changing only its number of distance bins.\"\"\"\n    \n        super().setup(1_000 if asv_pr_check_enabled() else 100_000, estimator)\n        self.n_lags = n_lags",
        "min_run_count": 2,
        "name": "asv_suite.variography.VariogramLagCount.time_operation",
        "number": 1,
        "param_names": [
            "n_lags",
            "estimator"
        ],
        "params": [
            [
                "24",
                "100",
                "256"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "ff933f3f4619fa27cdd17360bca2fe1e92621ff78786080cb8c4af0a3f9ae86f",
        "warmup_time": 0
    },
    "asv_suite.variography.VariogramLagCount.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass VariogramLagCount:\n    def setup(self, n_lags: int, estimator: str) -> None:\n        \"\"\"Keep the pair sample fixed while changing only its number of distance bins.\"\"\"\n    \n        super().setup(1_000 if asv_pr_check_enabled() else 100_000, estimator)\n        self.n_lags = n_lags",
        "name": "asv_suite.variography.VariogramLagCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_lags",
            "estimator"
        ],
        "params": [
            [
                "24",
                "100",
                "256"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "68cb99a6fa823c9ca47ac43a07f00c19df81687b9eb0f5f5949371f2f1fe9313"
    },
    "asv_suite.variography.VariogramPairCount.time_operation": {
        "code": "class _VariographyBenchmark:\n    def time_operation(self, *parameters: Any) -> None:\n        \"\"\"Compute the complete pair dataset or reduced variogram from prepared inputs.\"\"\"\n    \n        self._execute()\n\nclass VariogramPairCount:\n    def setup(self, n_pairs: int, estimator: str) -> None:\n        \"\"\"Prepare the same finite pairs for both estimators outside the measured call.\"\"\"\n    \n        _prepare_estimator(estimator)\n        self.pairs = prepare_variogram_pairs(n_pairs)\n        self.estimator = estimator\n        self.n_lags = 24",
        "min_run_count": 2,
        "name": "asv_suite.variography.VariogramPairCount.time_operation",
        "number": 1,
        "param_names": [
            "n_pairs",
            "estimator"
        ],
        "params": [
            [
                "10000",
                "100000",
                "1000000"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "repeat": 3,
        "rounds": 1,
        "sample_time": 0.01,
        "timeout": 300,
        "type": "time",
        "unit": "seconds",
        "version": "b0d78891e3ba7b000aab3571a62cbc790d2daa8c8674fde47a3531ae2e78c018",
        "warmup_time": 0
    },
    "asv_suite.variography.VariogramPairCount.track_peak_process_tree_mem_mb": {
        "code": "class _VariographyBenchmark:\n    def track_peak_process_tree_mem_mb(self, *parameters: Any) -> float:\n        \"\"\"Measure process memory including prepared inputs and the completed result.\"\"\"\n    \n        _, metrics = profile_call(self._execute, dask=False, include_children=True)\n        assert metrics.peak_process_tree_mem_mb is not None\n        return metrics.peak_process_tree_mem_mb\n\nclass VariogramPairCount:\n    def setup(self, n_pairs: int, estimator: str) -> None:\n        \"\"\"Prepare the same finite pairs for both estimators outside the measured call.\"\"\"\n    \n        _prepare_estimator(estimator)\n        self.pairs = prepare_variogram_pairs(n_pairs)\n        self.estimator = estimator\n        self.n_lags = 24",
        "name": "asv_suite.variography.VariogramPairCount.track_peak_process_tree_mem_mb",
        "param_names": [
            "n_pairs",
            "estimator"
        ],
        "params": [
            [
                "10000",
                "100000",
                "1000000"
            ],
            [
                "'matheron'",
                "'dowd'"
            ]
        ],
        "timeout": 300,
        "type": "track",
        "unit": "MB",
        "version": "1c097e48663cd37b8aa55c0010336525eb523e9eb8c704b6f589ab58eb27ab10"
    },
    "version": 2
}