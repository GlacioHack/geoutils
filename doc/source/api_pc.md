(api-pc)=
# The {class}`pc <geoutils.PointCloudAccessor>` accessor

The {class}`pc <geoutils.PointCloudAccessor>` accessor exposes GeoUtils point cloud methods on {class}`geopandas.GeoDataFrame` objects.

It mirrors the API of {class}`~geoutils.PointCloud`; native GeoPandas methods remain available directly on the dataframe.

**For the main point cloud API reference page, see {ref}`pointcloud-api`.**
Below is the full class description specific to {class}`pc <geoutils.PointCloudAccessor>`:

```{eval-rst}
.. currentmodule:: geoutils

.. autoclass:: PointCloudAccessor
    :members:
    :inherited-members:
    :undoc-members: false
    :show-inheritance:
```
