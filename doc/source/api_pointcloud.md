(api-pointcloud)=
# PointCloud

{class}`~geoutils.PointCloud` is the GeoUtils object for point clouds.
Its API is mirrored in the {class}`pc <geoutils.PointCloudAccessor>` accessor on {class}`geopandas.GeoDataFrame` objects.

**For the main point cloud API reference page, see {ref}`pointcloud-api`.**
Below is the full class description specific to {class}`~geoutils.PointCloud`:

```{eval-rst}
.. currentmodule:: geoutils

.. autoclass:: PointCloud
    :members:
    :inherited-members:
    :undoc-members: false
    :no-index:
    :show-inheritance:
```
